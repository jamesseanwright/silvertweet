<UserControl x:Class="SilverTweet.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" xmlns:sdk="http://schemas.microsoft.com/winfx/2006/xaml/presentation/sdk">

    <Grid Background="#FF5C7590" ShowGridLines="True">
        <Grid.RowDefinitions>
            <RowDefinition Height="40" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <Grid Grid.Row="0" Margin="7" ShowGridLines="True">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="150" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="50" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" CornerRadius="10" Background="#FFDEDEDE" Margin="0, 0, 5, 0">
                <TextBlock Text="Search Twitter" Foreground="#FF14517B" Margin="10, 3, 0, 0" />
            </Border>
            
            <TextBox Name="TxtSearch" Grid.Column="1" FontSize="14" Text="Topic..." />
            
            <Button Name="BtnSearch" Grid.Column="2" Content="Search" Click="BtnSearch_Click" />
        </Grid>
        <sdk:DataGrid x:Name="TweetList" Grid.Row="1" Margin="5">
        </sdk:DataGrid>
    </Grid>
</UserControl>